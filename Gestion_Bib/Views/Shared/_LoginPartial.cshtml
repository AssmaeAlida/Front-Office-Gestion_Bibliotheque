@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
      <li class="nav-item">
                                <a class="nav-link3 " asp-area="" asp-controller="Historique" asp-action="Historique">Historique</a>
       </li>
    <li class="nav-item">
        <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity?.Name!</a>
    </li>

    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button class="btn-logout" type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
}
else
{
         
        <li>
            <a class="btn-seconnecter " asp-area="Identity" asp-page="/Account/Login"> Se connecter </a>
        </li>
                <li class="btn-identifier">
                <a class="titreidentif" asp-area="Identity" asp-page="/Account/Register"> S'identifier </a>
            </li>
            
}
</ul>
<style>
    * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

.navbar-nav{
    display: flex;
    flex-direction: column;
    padding-left: 0;
    margin-bottom: 0;
    list-style: none;
    align-content: center;
    align-items: center;
}
.titreidentif{
        color: #FFF;
    font-family: Be Vietnam Pro;
    font-size: 16px;
    font-style: normal;
    font-weight: 600;
    margin-left: 31px;
    margin-top: 12px;
}
.btn-logout{
        width: 153px !important;
            height: 44px !important;
            flex-shrink: 0;
            border-radius: 5px;
            background: #6D98ED;
            box-shadow: 0px 22px 44px 0px rgba(0, 0, 0, 0.05);
            color: #FFF;
            font-family: Be Vietnam Pro;
            font-size: 16px;
            font-style: normal;
            font-weight: 600;
            margin-right: 20px;
            line-height: normal;
           

              
}
    .btn-seconnecter{
        color: #6D98ED;
        font-family: Be Vietnam Pro;
        font-size: 19px;
        font-style: normal;
        font-weight: 600;
        line-height: normal;
          margin-right: 20px;
    }
     .btn-identifier{
            width: 153px !important;
            height: 44px !important;
            flex-shrink: 0;
            border-radius: 5px;
            background: #6D98ED;
            box-shadow: 0px 22px 44px 0px rgba(0, 0, 0, 0.05);
            color: #FFF;
            font-family: Be Vietnam Pro;
            font-size: 16px;
            font-style: normal;
            font-weight: 600;
            margin-right: 20px;
            line-height: normal;
            display: flex;

     } 
     .nav-link3 {
    margin-right: 154px;
    margin-left: 107px;
    color: #000;
    font-family: Be Vietnam Pro;
    font-size: 19px;
    font-style: normal;
    font-weight: 400;
    line-height: normal;
    /* flex: auto; */
    /* display: flex; */
    /* flex-wrap: nowrap; */
    /* flex-direction: row-reverse; */
}
.navbar-light .navbar-nav .nav-link {
    color: rgba(0,0,0,.55);
    margin-left: -55px;
}
</style>